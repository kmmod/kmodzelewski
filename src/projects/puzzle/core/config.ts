export const gemColor = (id: number): string => {
  const colors = [
    "#ff0000",
    "#26ff00",
    "#0099ff",
    "#ff8c00",
    "#6100ff",
    "#ffffff",
    "#5f5f5f",
  ];

  return colors[id];
};

export const maxColors = 4;
